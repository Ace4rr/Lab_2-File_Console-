# Лабораторная работа

## Описание

Данный проект представляет собой консольное приложение миниоболочку с файловыми командами, реализованную на Python в рамках **Лабораторной работы №2**

### Поддерживаемые возможности (Easy)

* **Навигация по каталогам**: cd, включая `..` и `~`.
* **Список файлов:** ls с опцией `-l` для детального отображения.
* **Вывод файла:** Вывод содержимого файла в консоль.
* **Переход в другие директории:** поддержка.
* **Копирование и переименование, перемещение файлов и каталогов:** `cp`, `mv` с поддержкой рекурсировного копирования директорий.
* **Удаление файлов и директорий** Удаление файлов и директорий с подтверждением пользователя.

## Дополнительные возможности (Medium)
* **Поиск по содержимому файлов**: `grep` для поиска текста в файлах с поддержкой регулярных выражений.
* **Создание TAR-архивов**: `tar` для упаковки директорий в сжатые `tar.gz` архивы.
* **Распаковка TAR-архивов**: `untar` для извлечения содержимого `tar` и `tar.gz` архивов в текущую директорию.
* **Создание ZIP-архивов**: `zip` для упаковки директорий в `zip` архивы с сжатием.
* **Распаковка ZIP-архивов**: `unzip` для извлечения содержимого `zip` архивов в текущую директорию.

### Структура проекта
 <pre>
.
├── LAB_2-File_Console-                        # Корневая папка проекта
│   ├── src/
│   │   ├── commands/                          # Реализация команд оболочки
│   │   │   ├── base_command.py                # Абстрактный класс для команд
│   │   │   ├── cd.py                          # Движение по каталогам
│   │   │   ├── cat.py                         # Вывод содержимого в консоль
│   │   │   ├── ls.py                          # Вывод списка файлов каталога
│   │   │   ├── cp.py                          # Копирование файлов
│   │   │   ├── mv.py                          # Перемещение файлов
│   │   │   ├── rm.py                          # Удаление файлов
│   │   │   ├── zip.py                         # Архивирование файлов в .zip
│   │   │   ├── tar.py                         # Архивирование файлов в .tar
│   │   │   ├── unzip.py                       # Разархивирование файлов из .zip
│   │   │   ├── untar.py                       # Разархивирование файлов из .tar
│   │   │   ├── grep.py                        # Поиск по содержимому
│   │   │   ├── __init__.py
│   │   │
│   │   ├── core/
│   │   │   ├── argparser.py                   # argparser
│   │   │   ├── logger.py                      # Настройки логгера
│   │   │   ├── __init__.py
│   │   │
│   │   ├── data/
│   │   │   ├── .trash                         # Директория с удалёнными файлами для Undo (в близжайшем будущем)
│   │   │   ├── shell.log                      # Файл логов
│   │   │
│   │   ├── main.py                            # Точка входа в приложение
│   │   ├── __init__.py
│   │
│   ├── tests/                                 # Модульные тесты (pytest)
│   │   ├── conftest.py                        # Общие фикстуры
│   │   ├── test_cd.py                         # Тест cd
│   │   ├── test_cat.py                        # Тест cat
│   │   ├── test_ls.py                         # Тест ls
│   │   ├── test_cp.py                         # Тест cp
│   │   ├── test_mv.py                         # Тест mv
│   │   ├── test_rm.py                         # Тест rm
│   │   ├── test_zip.py                        # Тест zip
│   │   ├── test_tar.py                        # Тест tar
│   │   ├── test_unzip.py                      # Тест unzip
│   │   ├── test_untar.py                      # Тест untar
│   │   ├── test_grep.py                       # Тест grep
│   │   ├── test_parser.py                     # Тест аргпарсера
│   │
│   ├── .gitignore                             # Игнорируемые файлы
│   ├── .pre-commit-config.yaml
│   ├── README.md                              # Документация
</pre>

### Формат ввода
- Вводится команда оболочки (ls, cd, cat, cp, mv, rm и тп).
- Опции команд (-l, -r и тп) задаются через пробел.
- Поддерживаются как относительные, так и абсолютные пути.
- История команд логируется.

### Алгоритм работы
Пользователь вводит команду в minishell>.
Введённая строка парсится через argparse.
Вызывается соответствующий обработчик команды из модуля commands/.
Обработчик выполняет действие.
Все команды и ошибки логируются в shell.log.
Результаты выводятся на экран.
